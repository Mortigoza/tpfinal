package org.adaitw.tp_final.controller;

/*import org.adaitw.tp_final.model.entity.ConsejoEntity;
import org.adaitw.tp_final.service.ConsejoService;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RestController;

@RestController
public class ConsejoController {
    @Autowired
    private ConsejoService consejoService;

    @PostMapping("/enviar/consejo")
    public String sendMail(@RequestBody ConsejoEntity consejo)
    {
        String status
                = consejoService.sendSimpleMail(consejo);

        return status;
    }
}
*/