package org.adaitw.tp_final.repository;

import org.adaitw.tp_final.model.entity.RespuestasEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RespuestasRepository extends JpaRepository<RespuestasEntity, Long> {
}
